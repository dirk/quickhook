name: release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release'
        type: string
        required: true

jobs:
  build:
    uses: ./.github/workflows/release-build.yml
    with:
      tag: ${{ inputs.tag }}
